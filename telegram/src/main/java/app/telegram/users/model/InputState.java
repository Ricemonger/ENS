package app.telegram.users.model;

public enum InputState {
    BASE,
    USERNAME,
    PASSWORD,
    CONTACT_METHOD,
    CONTACT_ID,
    NOTIFICATION_NAME,
    CUSTOM_PHRASE, TASK_TIME, TASK_TYPE, TASK_NAME, NOTIFICATION_TEXT
}
